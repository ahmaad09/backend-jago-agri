let createPool;_f48‍.x([["getConnection",()=>getConnection]]);_f48‍.w("mysql2/promise",[["createPool",["createPool"],function(v){createPool=v}]]);

       const getConnection = async () => {
    try {
        const poll = createPool({
            host: "localhost",
            user: "root",
            password: "",
            database: "db_jago_agri"
        })

        return poll;
    } catch (error) {
        _f48‍.g.console.log(error);
    }
}